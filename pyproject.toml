[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "viraldzen"
version = "0.1.0"
description = "Collect viral Dzen (Дзен) content without a browser"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Артур Хорошев" }]
keywords = ["dzen", "cursor", "agent-skills", "viral"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Horosheff/ViralDzen"
Issues = "https://github.com/Horosheff/ViralDzen/issues"

[project.scripts]
viraldzen = "viraldzen.cli:main"

[tool.setuptools]
packages = ["viraldzen"]
